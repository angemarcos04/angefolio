# The current static site requires no secrets.
# Reserve environment variables for documented, non-sensitive configuration only.
# The canonical placeholder URL currently lives in astro.config.mjs and src/lib/data/site.ts.
